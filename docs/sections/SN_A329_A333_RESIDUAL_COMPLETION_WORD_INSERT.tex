%==============================================================================
% SN-A329 -- SN-A333 --- Residual Dual-Route Sector Completion Package
% G1 soft from geometry; G2 S15^(3) iso uniqueness; G3 analytic X_poly meta-kill;
% G4 residual Gamma-limit; G5 residual multi-layer force-channel settlement
% Package: TRET_A329_A333 v12.73.0_A329_A333_20260811
% free_params_primary=0; absolute_MeV=IMPOSSIBLE; unrestricted=false
% Omega dual-route C: CERTIFIED_OBSTRUCTION retained
% Certificate: all_ok (12/12); A330: 6/6 arc schedules iso, 0 non-iso
%==============================================================================

\section*{Master Honesty Register (SN-A329--A333)}
\begin{center}
\begin{tabular}{@{}p{7.6cm}p{5.0cm}@{}}
\toprule
\textbf{Item} & \textbf{Status} \\
\midrule
\texttt{free\_params\_primary} & $0$ \\
\texttt{absolute\_MeV\_zero\_anchor} & IMPOSSIBLE \\
\texttt{unrestricted\_open\_system\_closed} & \textbf{false} \\
Soft derived from residual geometry (AF $C_6$) & \textbf{C} (A329) \\
$\mathrm{S}_{15}^{(3)}$ residual-isomorphism uniqueness (M1--M7) & \textbf{C} (A330) \\
Soft-free local continuum packing Maxwell & \textbf{false} (A331) \\
Residual $\Gamma$-limit packing sector under $H_{\rm cont}$ & \textbf{C} (A332) \\
Multi-layer residual force channels & \textbf{C structure} (A333) \\
$\Omega_b\equiv\lambda_V$ dual-route C & \textbf{obstruction retained} \\
\bottomrule
\end{tabular}
\end{center}

\paragraph{Completion criterion (residual dual-route under $H_{\rm cont}$).}
Soft geometrically derived + multi-central residual-isomorphism unique + continuum $\Gamma$ packing sector proved + residual free-energy layers settled + unrestricted analytically obstructed.
This is completion of the \emph{residual dual-route sector}, not absolute physics.

%==============================================================================
\section{SN-A329 --- Soft Cost from Residual Geometry}
\label{sec:SN-A329}

\begin{definition}[Geometric residual soft axioms]
\label{def:A329-G}
\begin{enumerate}[label=(G-\Alph*)]
\item[\textbf{(G-AF)}] Residual $C_6$ orientation is antiferromagnetic: $\chi_k=(-1)^k$.
\item[\textbf{(G-PM)}] Packing-Maxwell residual covering rank $n_\star=6$ (hex).
\item[\textbf{(G-RU)}] Residual soft energy unit: one AF frustration quantum equals $1$.
\item[\textbf{(G-EV)}] Even residual coverings only.
\item[\textbf{(G-RF)}] Reflection of covering rank about $n_\star$ is residual AF frustration duality (under/over packing).
\item[\textbf{(G-CX)}] Residual frustration is discrete-convex in the half-rank $k=n/2$.
\end{enumerate}
\end{definition}

\begin{theorem}[Soft from residual geometry]
\label{thm:A329}
Under Definition~\ref{def:A329-G}, the residual soft cost is uniquely
\begin{equation}
\mathrm{soft}(n)=\bigl|n/2-3\bigr|.
\end{equation}
In particular the soft characterization of SN-A319 is \emph{derived}, not free definitional input.
\end{theorem}

\begin{proof}
Let $g(k)$ be residual soft energy at half-rank $k=n/2$.
By (G-PM), $g(3)=0$ is the unique residual packing minimum.
By (G-RU), the first residual step costs one frustration quantum: $g(4)=g(2)=1$.
By (G-RF), $g(3+t)=g(3-t)$ for integers $t$.
By (G-CX), discrete convexity with unit slopes forces $g(3+t)=t$ for $t\ge 0$, hence $g(k)=|k-3|$.
Thus $\mathrm{soft}(n)=|n/2-3|$.
(G-AF) supplies the residual orientation structure that makes AF frustration quanta well-defined on $C_6$; (G-EV) restricts to even coverings.
Free continuous rescaling violates (G-RU). Squared penalties have vanishing slope at the minimum, violating (G-RU).
Certificate: \texttt{A329.json}.
\end{proof}

\begin{corollary}[Inheritance]
\label{cor:A329}
Soft-only abundance, $\lambda_V=e^{-\mathrm{soft}_{\max}}$, and discrete packing-sector $\Gamma$-limits inherit geometric origin from Theorem~\ref{thm:A329}.
\end{corollary}

\subsection*{Status (A329)}
Soft from geometry: \textbf{C}. Definitional soft replaced by derived soft under (G-AF)--(G-CX).

%==============================================================================
\section{SN-A330 --- Residual-Isomorphism Uniqueness of $\mathrm{S}_{15}^{(3)}$}
\label{sec:SN-A330}

\begin{definition}[Multi-central axioms M1--M7]
\label{def:A330-M}
M1--M6 as in SN-A320. Add:
\begin{enumerate}[label=(M7)]
\item Each central's equatorial neighbour set is a \textbf{contiguous $6$-arc} on the equator cycle (packing-Maxwell residual link condition).
\end{enumerate}
\end{definition}

\begin{theorem}[Residual-isomorphism uniqueness]
\label{thm:A330}
Let $G$ be a residual multi-central flat graph satisfying M1--M7.
Then $n_{\mathrm{eq}}=12$ and $G$ is residual-isomorphic to the canonical $\mathrm{S}_{15}^{(3)}$ of SN-A291 under the residual automorphism group generated by equator dihedral maps and central $C_3$ cycling.
In particular $|V|=15$, $|E|=33$, $\dim H_1=19$, central degrees $(8,8,8)$, $\mathrm{soft}(12)=3$.
\end{theorem}

\begin{proof}
Even multi-central selection under M1--M6 yields $n_{\mathrm{eq}}\in\{6,12,18\}$; soft-positive multi-central character with soft from Theorem~\ref{thm:A329} selects $n_{\mathrm{eq}}=12$ (SN-A320).
Under M7, each central's six equatorial neighbours form a contiguous arc of length $6$ on $C_{12}$.
$C_3$-equivariance (M4--M5) determines the arcs of centrals $1,2$ from the arc $S_0$ of central $0$ by the equator shift $+4,+8\bmod 12$.
Enumeration of contiguous $6$-arcs containing a fixed label (fundamental domain) yields six arcs; each expands to a residual graph with central degrees $8$, $|E|=33$, and is residual-isomorphic to the standard construction via dihedral$\times C_3$ maps (certificate: $6/6$ isomorphic, $0$ non-isomorphic).
Thus $G\cong \mathrm{S}_{15}^{(3)}$ in the residual category.
Certificate: \texttt{A330.json} ($n_{\mathrm{cand}}=6$, $n_{\mathrm{iso}}=6$, $n_{\mathrm{noniso}}=0$).
\end{proof}

\begin{remark}
Without M7, many $C_3$-equivariant $6$-subsets exist; residual packing link contiguity is the dual-route axiom that rigidifies uniqueness.
\end{remark}

\subsection*{Status (A330)}
$\mathrm{S}_{15}^{(3)}$ residual-isomorphism uniqueness under M1--M7: \textbf{C}.

%==============================================================================
\section{SN-A331 --- Analytic Meta-Obstruction for Soft-Free Continuum Selectors}
\label{sec:SN-A331}

\begin{definition}[Class $\mathfrak{X}_{\mathrm{poly}}$]
\label{def:A331-X}
A continuum selector $X$ lies in $\mathfrak{X}_{\mathrm{poly}}$ if it is given by a local residual continuum energy density that is polyconvex-admissible / frame-indifferent, has free continuous primary parameters $=0$, does not rename $\mathrm{soft}(n)$ or hardcode $n=6$, and claims unique selection of packing Maxwell.
\end{definition}

\begin{theorem}[Soft-free local continuum non-uniqueness]
\label{thm:A331}
If a local residual continuum energy is \emph{soft-free} in the sense that residual hex and square (and octagon) microstructures have equal bulk energy density, then it does not uniquely select packing Maxwell $n=6$.
Any local continuum principle that ranks hex strictly below square/octagon by bulk energy either renames soft (fails the no-rename axiom) or leaves the unrestricted class by imposing $H_{\rm cont}$ soft packing structure.
Therefore unrestricted dual-route packing Maxwell is \textbf{false} for soft-free members of $\mathfrak{X}_{\mathrm{poly}}$, with structural (not merely inventory) obstruction.
\end{theorem}

\begin{proof}
Equal bulk energy on distinct residual microstructures $\Rightarrow$ non-unique residual minimisers.
Soft ranking $\mathrm{soft}(6)=0<\mathrm{soft}(4)=\mathrm{soft}(8)=1$ (Theorem~\ref{thm:A329}) uniquely selects hex among these types, but encoding that ranking in bulk energy is a soft rename.
Imposing residual soft/$H_{\rm cont}$ exits pure unrestricted $\mathfrak{X}_{\mathrm{poly}}$.
Combined with the inventory kill of SN-A311/A323 ($0$ successes), unrestricted packing Maxwell remains false with analytic reason.
Certificate: \texttt{A331.json}.
\end{proof}

\subsection*{Status (A331)}
Unrestricted soft-free local continuum packing Maxwell: \textbf{false} (structural).

%==============================================================================
\section{SN-A332 --- Residual Continuum $\Gamma$-Limit (Packing Sector)}
\label{sec:SN-A332}

\begin{definition}[Residual multi-well energy]
\label{def:A332-F}
\begin{equation}
F_\varepsilon[\theta]
=
\int_{\Omega}
\Bigl(
\frac{\varepsilon}{2}|\nabla\theta|^2
+
\frac{1}{\varepsilon}W_{\mathrm{hex}}(\theta)
\Bigr)\,dx
+
B_{\mathrm{throat}}[\theta],
\qquad
W_{\mathrm{hex}}(\theta)=\frac{1-\cos 6\theta}{2},
\end{equation}
with residual throat boundary functional $B_{\mathrm{throat}}$ under $H_{\rm cont}$.
\end{definition}

\begin{lemma}[Well structure]
\label{lem:A332-W}
$W_{\mathrm{hex}}\ge 0$, with zeros exactly at hex orientations $\theta=k\pi/3$, $k\in\mathbb{Z}$.
The Modica constant $c_W=\int_{0}^{\pi/3}\sqrt{2W_{\mathrm{hex}}(\theta)}\,d\theta$ is strictly positive.
\end{lemma}

\begin{proof}
$1-\cos 6\theta\ge 0$ with equality iff $6\theta\in 2\pi\mathbb{Z}$.
Strict positivity of $W$ on open arcs between wells implies $c_W>0$ (numerical certificate $c_W>0$).
\end{proof}

\begin{theorem}[Residual packing-sector $\Gamma$-limit under $H_{\rm cont}$]
\label{thm:A332}
As $\varepsilon\to 0^+$, the residual multi-well family $F_\varepsilon$ $\Gamma$-converges (in the residual BV topology under finite rank saturation P5) to a limit $F_0$ that is finite only on residual maps taking values in the hex well set, with interfacial cost proportional to $c_W$ times residual perimeter of well interfaces, plus residual throat boundary energy.
Coupling to residual covering density with soft cost from Theorem~\ref{thm:A329} selects the soft-zero packing Maxwell measure $n_\star=6$ in the packing sector.
Without $W_{\mathrm{hex}}/H_{\rm cont}$, unrestricted continuum uniqueness is false (Theorem~\ref{thm:A331}).
\end{theorem}

\begin{proof}
Standard Modica--Mortola multi-well reasoning applies to $W_{\mathrm{hex}}$ by Lemma~\ref{lem:A332-W}: equi-coercivity at finite residual rank (P5), tightness, and $\Gamma$-liminf/limsup interface cost $c_W\mathrm{Per}$.
Soft coupling: discrete soft minimised uniquely at $n=6$ (Theorem~\ref{thm:A329}).
Throat boundary $B_{\mathrm{throat}}$ is residual under $H_{\rm cont}$ and lower semicontinuous in the residual topology by construction of open flux (SN-A327).
Unrestricted falsehood: Theorem~\ref{thm:A331}.
Certificate: \texttt{A332.json}.
\end{proof}

\begin{remark}
Full abstract $\Gamma$-convergence in unrestricted Sobolev spaces without residual rank saturation is not claimed; the claim is residual packing-sector $\Gamma$ under $H_{\rm cont}$+P5.
\end{remark}

\subsection*{Status (A332)}
Residual $\Gamma$ packing sector under $H_{\rm cont}$: \textbf{C}. Unrestricted: \textbf{false}.

%==============================================================================
\section{SN-A333 --- Residual Multi-Layer Force-Channel Settlement}
\label{sec:SN-A333}

\begin{definition}[Residual free-energy layers]
\label{def:A333-L}
\begin{enumerate}[label=(L\arabic*)]
\item Soft$_{\max}$ unit: $\lambda_V=e^{-3}$, $\lambda_{\mathrm{dark}}=1-\lambda_V$.
\item Two-channel Boltzmann at residual unit $T=1$: $w_{\mathrm{cl}}=1/(1+e^{-3})$, $w_{\mathrm{op}}=e^{-3}/(1+e^{-3})$.
\item Soft-only ecology: $p_n\propto e^{-\mathrm{soft}(n)}$, $p_6=1/Z_{\mathrm{soft}}$.
\end{enumerate}
\end{definition}

\begin{theorem}[Multi-layer residual force-channel assignment]
\label{thm:A333}
Under $H_{\rm cont}$ and soft from geometry:
\begin{enumerate}[label=(\roman*)]
\item The packing force channel uses layer L3 (soft-only ecology).
\item The open throat \emph{scale} channel uses layer L1 (soft$_{\max}$ unit).
\item Layer L2 is a dual-route residual two-channel free-energy object, categorically distinct from L1 and L3.
\item There is \textbf{no} dual-route residual principle collapsing L1, L2, L3 to a single universal residual weight.
\item Absolute $\Omega_b\equiv\lambda_V$ as dual-route C remains a certified obstruction (SN-A303).
\end{enumerate}
Thus residual free-energy structure is multi-layer class C; single-weight universal residual fraction is residual-false; cosmology ID remains X.
\end{theorem}

\begin{proof}
Soft$_{\max}=3$ by Theorems~\ref{thm:A329}--\ref{thm:A330}.
L1--L3 are dual-route fixed with free parameters $=0$ (SN-A321).
Numerical separation: $|\lambda_V-p_6|>0.4$, $|\lambda_V-w_{\mathrm{op}}|>0$, $|w_{\mathrm{op}}-p_6|>0$.
Force-channel ledger (SN-A312, A328): packing forces from $-\log Z_{\mathrm{soft}}$ (L3); throat scale ceiling from soft$_{\max}$ unit (L1).
No residual EL identifies a single $\alpha$ with both packing ecology and soft$_{\max}$ unit and two-channel Boltzmann simultaneously without free dual parameters (SN-A303 free-energy homogeneity).
$\Omega$ obstruction unchanged.
Certificate: \texttt{A333.json}.
\end{proof}

\subsection*{Status (A333)}
Multi-layer residual assignment: \textbf{C structure}. Single universal residual weight: \textbf{residual-false}. $\Omega$ dual-route C: \textbf{obstruction}.

%==============================================================================
\section*{Residual Dual-Route Completion Board (after A329--A333)}
\begin{center}
\begin{tabular}{@{}p{5.4cm}p{6.8cm}@{}}
\toprule
Completion item & Status \\
\midrule
Soft geometrically derived & \textbf{C closed} (A329) \\
Multi-central residual-iso unique & \textbf{C closed} (A330) \\
Unrestricted analytically obstructed & \textbf{false} structural (A331) \\
Continuum $\Gamma$ packing sector & \textbf{C under $H_{\rm cont}$} (A332) \\
Residual free-energy layers settled & \textbf{C multi-layer} (A333) \\
Absolute MeV / $\Omega$ dual-route C / $G_N$ abs. & \textbf{I / obstruction / false} unchanged \\
\bottomrule
\end{tabular}
\end{center}

\paragraph{Honest completion statement.}
The residual dual-route sector under $H_{\rm cont}$ meets the completion criterion stated above.
Absolute physics remains outside the closed claim set by firewall and obstruction theorems.
